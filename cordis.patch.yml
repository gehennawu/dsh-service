# dsh-service bundle patch
# host 半（main → index.js）：注册 /dsh-service RPC 通道（loopback 信任栅栏）
# client 半（dsh.client → exports["./client"] → client.js）：设置页「服务控制」
- insert:
    - id: dsh-service
      name: '@gehennawu/dsh-service'